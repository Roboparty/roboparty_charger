
// SPDX-License-Identifier: GPL-3.0
// Copyright (C) 2026 zz020


/**
 * @file modbus_master.c
 * @brief Modbus RTU ??API??,????????????????
 * @note ??????????????MCU????
 */

#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include "main.h"
#include "usart.h"
#include "tws_bms.h"

/* ???????????????,??????????? */
//extern bool UART_Transmit(const uint8_t *data, uint16_t len);      /* ????,????true */
//extern uint16_t UART_Receive(uint8_t *buffer, uint16_t max_len, uint32_t timeout_ms); /* ????,??????? */

/* CRC??????(?????) */
uint16_t modbus_crc16(const uint8_t *pucFrame, uint16_t usLen);

/**
 * @brief ?????????????(???0x03)
 * @param slave_addr     ????(1~247)
 * @param start_reg      ???????(16?)
 * @param reg_count      ?????????(??125,?????????<=250)
 * @param dest_buffer    ??????????(????? reg_count * 2 ??)
 * @return               0????,????????
 */
uint8_t Modbus_ReadHoldingRegisters(uint8_t slave_addr, uint16_t start_reg,
                                    uint16_t reg_count, uint16_t *dest_buffer, uint16_t recv_len)
{
    uint8_t request[8];      /* ?????8?? */
    uint8_t response[260];   /* ??????:??+??+???+250??+2CRC = 254,??? */
    uint16_t crc;
    uint8_t *ptr;
    uint8_t byte_count;
    uint16_t i;
    uint16_t calc_crc;

    /* ???? */
    if (dest_buffer == NULL || reg_count == 0 || reg_count > 125) {
        return MODBUS_ERR_PARAM;
    }

    /* ????? */
    request[0] = slave_addr;                     /* ???? */
    request[1] = 0x03;                           /* ???:?????? */
    request[2] = (uint8_t)(start_reg >> 8);      /* ???????? */
    request[3] = (uint8_t)(start_reg & 0xFF);    /* ???????? */
    request[4] = (uint8_t)(reg_count >> 8);      /* ???????? */
    request[5] = (uint8_t)(reg_count & 0xFF);    /* ???????? */
    crc = modbus_crc16(request, 6);              /* ??CRC(?6??) */
    request[6] = (uint8_t)(crc & 0xFF);          /* CRC??? */
    request[7] = (uint8_t)(crc >> 8);            /* CRC??? */

    if (HAL_UART_Transmit(&huart1, request, 8, HAL_MAX_DELAY) != HAL_OK) {
        printf("request send TIMEOUT;");
        return MODBUS_ERR_TX_FAIL;
    }

    uint16_t frame_len = 3 + recv_len + 2;

    if(HAL_UART_Receive(&huart1, response, frame_len, RX_TIMEOUT_MS) != HAL_OK){
//        printf("Response recv TIMEOUT: 0x%X, 0x%X, 0x%X\n", response[0], response[1], response[2]);
        return MODBUS_ERR_TIMEOUT;
    }

    if (response[0] != slave_addr) {
        printf("BMS Slave Address NOT MATCH: 0x%X;", response[0]);
        return MODBUS_ERR_SLAVE_ADDR;
    }
    if (response[1] != 0x03) {
        printf("BMS Response CMD NOT MATCH: 0x%X;", response[1]);
        return MODBUS_ERR_FUNC_CODE;
    }

    byte_count = response[2];
    if (byte_count != (reg_count * 2)) {
        printf("BMS Read PLD Len Mismatch;\n");
        return MODBUS_ERR_FRAME_LEN;
    }

    calc_crc = modbus_crc16(response, frame_len - 2);
    if ((calc_crc & 0xFF) != response[frame_len - 2] ||
        ((calc_crc >> 8) & 0xFF) != response[frame_len - 1]) 
    {
            printf("BMS Bad CRC -> 0x%X, Expected: 0x%X;\n", (response[frame_len - 2] | (response[frame_len - 1] << 8)), calc_crc);
            return MODBUS_ERR_CRC;
    }

    ptr = &response[3];
    for (i = 0; i < reg_count; i++) 
    {
        dest_buffer[i] = *(ptr+2*i) | (*(ptr+2*i+1) << 8);
    }

    return MODBUS_OK;
}

/**
 * @brief ??????????????(???0x06)
 * @param slave_addr     ????(1~247)
 * @param reg_addr       ?????(16?)
 * @param value          ????16???
 * @return               0????,????????
 */
uint8_t Modbus_WriteSingleRegister(uint8_t slave_addr, uint16_t reg_addr, uint16_t value)
{
    uint8_t request[8];      /* ?????8?? */
    uint8_t response[8];     /* ?????8??(??) */
    uint16_t crc;
    uint16_t recv_len;
    uint16_t calc_crc;

    /* ?????(??:??????????,?????,??????) */
    request[0] = slave_addr;                     /* ???? */
    request[1] = 0x06;                           /* ???:?????? */
    request[2] = (uint8_t)(reg_addr >> 8);       /* ???????? */
    request[3] = (uint8_t)(reg_addr & 0xFF);     /* ???????? */
    request[4] = (uint8_t)(value & 0xFF);        /* ?????(????) */
    request[5] = (uint8_t)(value >> 8);          /* ????? */
    crc = modbus_crc16(request, 6);              /* ??CRC(?6??) */
    request[6] = (uint8_t)(crc & 0xFF);          /* CRC??? */
    request[7] = (uint8_t)(crc >> 8);            /* CRC??? */

    /* ???? */
    if (!HAL_UART_Transmit(&huart1, request, 8, HAL_MAX_DELAY)) {
        return MODBUS_ERR_TX_FAIL;
    }

    /* ????(??8??) */
    recv_len = HAL_UART_Receive(&huart1, response, 8, RX_TIMEOUT_MS);
    if (recv_len < 8) {
        return MODBUS_ERR_TIMEOUT;
    }

    /* ?????????? */
    if (response[0] != slave_addr) {
        return MODBUS_ERR_SLAVE_ADDR;
    }
    if (response[1] != 0x06) {
        return MODBUS_ERR_FUNC_CODE;
    }

    /* ??CRC */
    calc_crc = modbus_crc16(response, 6);   /* ?6????CRC */
    if ((calc_crc & 0xFF) != response[6] ||
        ((calc_crc >> 8) & 0xFF) != response[7]) {
        return MODBUS_ERR_CRC;
    }

    /* ??:?????????????????? */
    if (response[2] != request[2] || response[3] != request[3] ||
        response[4] != request[4] || response[5] != request[5]) {
        /* ???????,????,???????????,????????? */
        /* ?????,???? */
        return MODBUS_ERR_FRAME_LEN;
    }

    return MODBUS_OK;
}

int ReadBMS_SOC(uint16_t * bat_soc)
{
       //--------------- Read BAT Soc -----------------//
    if(Modbus_ReadHoldingRegisters(0x01, 0x9028, 1, bat_soc, 2) == MODBUS_ERR_TIMEOUT)
        return 1;
    else
        return 0;
//        printf("BMS Connect TIMEOUT!;\n");
//    else
//        sprintf(strBmsSoc, "%d", BmsSoc[0]);
    
}
//        HAL_Delay(50);
int ReadBMS_SOH(uint16_t * bat_soh)
{
    //--------------- Read BAT Soh -----------------//
    if(Modbus_ReadHoldingRegisters(0x01, 0x9029, 1, bat_soh, 2) == MODBUS_ERR_TIMEOUT)
        return 1;
    else
        return 0;
//        printf("BMS Connect TIMEOUT!;\n");
//    else
//        sprintf(strBmsSoh, "%d", BmsSoh[0]);
}

/* ???modbus_crc16?????(???,???????) */
uint16_t modbus_crc16(const uint8_t *pucFrame, uint16_t usLen)
{
    static const uint8_t aucCRCHi[] = {
        
    };
    static const uint8_t aucCRCLo[] = {
        
    };

    uint8_t ucCRCHi = 0xFF;
    uint8_t ucCRCLo = 0xFF;
    int iIndex;
    uint16_t crclen = usLen;

    while (crclen--) {
        iIndex = ucCRCLo ^ *(pucFrame++);
        ucCRCLo = (uint8_t)(ucCRCHi ^ aucCRCHi[iIndex]);
        ucCRCHi = aucCRCLo[iIndex];
    }
    return (uint16_t)(ucCRCHi << 8 | ucCRCLo);
}
